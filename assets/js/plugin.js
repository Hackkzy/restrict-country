jQuery(document).ready(function ($) {
  jQuery("#country").multiselect({ columns: 1, placeholder: "Select Country" });
  jQuery("#rca_selected_country").multiselect({ columns: 1, placeholder: "Select Country" });
});
